name: app
type: "python:3.10"

build:
    commands:
        - pip install -r requirements.txt

web:
    commands:
        start: gunicorn wsgi:application
    locations:
        "/":
            root: ""
            passthru: false

mounts: {}
relationships: {}
