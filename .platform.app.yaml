name: app

type: "python:3.11"

hooks:
  build: |
    pip install -r requirements.txt

web:
  commands:
    start: python app.py
  locations:
    "/":
      root: ""
      passthru: false
      allow: true

mounts:
  "/data":
    source: local
    source_path: data

disk: 256
